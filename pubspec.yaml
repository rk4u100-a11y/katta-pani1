name: katta_pani
description: A new Flutter project for civil work management.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
   image_picker: ^1.0.7
   shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # ഇത് താഴെ കൃത്യമായി ചേർക്കുക:
  assets:
    - assets/images/